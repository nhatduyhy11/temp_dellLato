const sum = function (a, b) {
  return a + b;
};

const arrowSum = (a, b) => {
  return a + b;
};

const arrowSum2 = (a, b) => a + b;

const arrowSquare = (a) => a * a;

// console.log(sum(1, 2));
// console.log(arrowSum2(2, 3));
console.log(arrowSquare(10));

